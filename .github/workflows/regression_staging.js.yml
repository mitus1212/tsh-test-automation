name: Regression Tests
on:
  workflow_dispatch:
    inputs:
      ENV:
        required: true
        type: string
        default: "staging"
      BROWSER:
        required: true
        default: "chrome"
        type: choice
        options:
          - "chrome"
          - "firefox"
      TEST_CYCLE_ID:
        required: false
        type: string
        default: "Test-Cycle-1"
      TEST_CYCLE_NAME:
        required: true
        type: string
        default: "Automated Regression Tests"
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 18.18.2
      - name: Install
        run: npm install
      - name: "Create env file"
        run: |
          touch .env
          echo BASE_URL=${{ secrets.BASE_URL}} >> .env
          echo EMAIL=${{ secrets.EMAIL }} >> .env
          echo EMAIL_APIKEY=${{ secrets.EMAIL_APIKEY }} >> .env
          echo EMAIL_URL=${{ secrets.EMAIL_URL }} >> .env
          cat .env
      - name: Test
        run: npm run wdio
      - uses: actions/upload-artifact@v2
        if: failure()
        with:
          name: logs
          path: logs
